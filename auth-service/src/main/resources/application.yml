spring:
  datasource:
    driver-class-name: ${auth-server.datasource.driver-class-name}
    url: ${auth-server.datasource.url}
    username: ${auth-server.datasource.username}
    password: ${auth-server.datasource.password}